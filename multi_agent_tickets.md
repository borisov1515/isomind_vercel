# IsoMind Multi-Agent Ticket Breakdown

Этот документ декомпозирует проект IsoMind на конкретные тикеты для параллельной работы мульти-агентной системы. Каждая задача привязана к специализированному ИИ-агенту.

> [!IMPORTANT]
> **Паттерн Разработки (Vast-First):** Весь девелопмент и запуск агентов происходит **непосредственно на инстансах Vast.ai** через SSH. Локальные машины используются только (как тонкие клиенты) как IDE. Код регулярно синхронизируется в Git (через скрипты или Github Actions). Веса моделей (например, `/models/*.safetensors` или папки HuggingFace/ollama) жестко исключены через `.gitignore`.

## Участвующие Агенты (Роли)
- 👤 **Agent Human (Вы):** Аренда серверов, биллинг, прокидывание доступов (SSH-ключи), управление бюджетом и финальный апрув.
- 🏗️ **Agent InfraOps:** Инфраструктура, Docker, Linux, WebRTC, Vast.ai SSH & Git Sync.
- 👁️ **Agent VisionQA:** Интеграция VLM (Qwen-VL), промпт-инжиниринг, векторная база данных (RAG), обрезка изображений.
- 🤖 **Agent Automation:** Управление браузером (Playwright), Stealth-модули, эмуляция мыши/клавиатуры (xdotool/PyAutoGUI).
- 🌐 **Agent Fullstack:** Платформенная часть (Next.js Dashboard, FastAPI Orchestrator, Billing).

---

## 🚦 Эпик 0: Настройка Процесса Разработки (DevEx)
*Фундамент для работы всех агентов.*

### [TICKET-0.1] Аренда Инстанса и Настройка SSH (Manual)
- **Агент:** 👤 Agent Human
- **Тип:** Sequential (Блокирует всё остальное)
- **Задача:** Зарегистрироваться на Vast.ai, пополнить баланс, закинуть свой открытый SSH-ключ в аккаунт. Арендовать инстанс (желательно RTX 3090/4090 с образом `nvidia/cuda:12.1.1-base-ubuntu22.04` или дефолтным Ubuntu). Получить IP и Порт для подключения по SSH.
- **Критерий приемки:** Вы можете зайти на удаленную машину командой `ssh -p <PORT> root@<IP>`.

### [TICKET-0.2] Инициализация репозитория и .gitignore
- **Агент:** 🏗️ InfraOps
- **Тип:** Parallel (с TICKET-0.1)
- **Задача:** Настроить Git-репозиторий. Создать `.gitignore`, который железобетонно исключает многогигабайтные веса моделей LLM/VLM (`.gguf`, `.safetensors` и кэши), временные файлы плейрайта, логи Xvfb и папки `chromium_data`.
- **Критерий приемки:** Пуш в удаленный репо проходит за секунды, без залипаний на отправке файлов на 20GB.

### [TICKET-0.3] Скрипты синхронизации Antigravity <-> Vast.ai
- **Агент:** 🏗️ InfraOps
- **Тип:** Sequential (Требует TICKET-0.1)
- **Задача:** Написать shell-скрипты (или Github Actions workflow) для бесшовного деплоя кода на Vast.ai по SSH при сохранении файлов локально (через `rsync` или `git pull` на удаленном сервере).
- **Критерий приемки:** Скрипт `deploy.sh` обновляет код на Vast.ai и перезапускает Docker-контейнер/FastAPI за считанные секунды.

---

## 🚦 Эпик 1: Базовая Песочница (The Shell)
*Этот этап должен быть завершен первым. Задачи можно делать параллельно.*

### [TICKET-1.1] Сборка и деплой Docker-образа (NVIDIA + Xvfb)
- **Агент:** 🏗️ InfraOps
- **Тип:** Sequential (Блокирует `TICKET-1.3`, `TICKET-2.1`)
- **Задача:** Настроить автоматический билд образа на основе `infrastructure/Dockerfile`. Написать скрипт развертывания на инстансе Vast.ai через их CLI/API.
- **Критерий приемки:** Контейнер успешно стартует на Vast.ai, Xvfb запущен, работает `nvidia-smi`.

### [TICKET-1.2] Подготовка FastAPI Orchestrator Backend
- **Агент:** 🌐 Fullstack
- **Тип:** Parallel
- **Задача:** Расширить `agent_api/main.py`. Написать Pydantic модели для всех ручек из `data_models.md` (взаимодействие с песочницей). 
- **Критерий приемки:** API поднимается локально, Swagger UI доступен, ручки возвращают 200 OK.

### [TICKET-1.3] Настройка трансляции (WebRTC / noVNC)
- **Агент:** 🏗️ InfraOps
- **Тип:** Sequential (Зависит от `TICKET-1.1`)
- **Задача:** Настроить безопасный стриминг Xvfb (:99) наружу. Внедрить `selkies-gstreamer` (для WebRTC) или хотя бы надежный WebSocket мост для noVNC с парольной защитой.
- **Критерий приемки:** Можно зайти через браузер хоста и увидеть рабочий стол Openbox.

---

## 🚦 Эпик 2: Восприятие и Действие (Senses & Action)
*После завершения Эпика 1. Агенты работают параллельно внутри контейнера.*

### [TICKET-2.1] API управления и Stealth браузер
- **Агент:** 🤖 Automation
- **Тип:** Parallel (Зависит от `TICKET-1.1`, `TICKET-1.2`)
- **Задача:** Написать скрипт на Python, который по командам от `agent_api` запускает Chromium через Playwright с плагином `playwright-stealth`. 
- **Критерий приемки:** Вызов `POST /navigate` реально открывает страницу в Xvfb. Тест на сloudflare/bot-защиту проходит.

### [TICKET-2.2] Эмуляция человеческой мыши
- **Агент:** 🤖 Automation
- **Тип:** Parallel (Зависит от `TICKET-1.2`)
- **Задача:** Реализовать логику движения курсора по кривым Безье к заданным координатам `(X, Y)`. Добавить микро-паузы перед кликами.
- **Критерий приемки:** Вызов `POST /mouse/click` визуально отображает плавное движение мыши в VNC и клик.

### [TICKET-2.3] Захват экрана и модуль "Зрения"
- **Агент:** 👁️ VisionQA
- **Тип:** Parallel
- **Задача:** Написать модуль, который делает высокоскоростной скриншот нужной области (Bounding Box), переводит его в Base64 и дергает локальный инстанс Ollama/vLLM (Moondream/Qwen-VL).
- **Критерий приемки:** Модуль может за секунду вернуть текстовое описание "что находится в координатах X,Y".

---

## 🚦 Эпик 3: Память и Обучение (Visual RAG & Blueprints)
*Требует завершения `TICKET-2.3`.*

### [TICKET-3.1] Развертывание PostgreSQL и pgvector
- **Агент:** 🌐 Fullstack
- **Тип:** Parallel
- **Задача:** Поднять базу данных, написать SQLAlchemy модели для `AgentProfiles`, `VisualAnchors`, `Blueprints`. Накатить миграции.

### [TICKET-3.2] Логика извлечения "Визуальных Якорей"
- **Агент:** 👁️ VisionQA
- **Тип:** Sequential (Зависит от `TICKET-2.3`, `TICKET-3.1`)
- **Задача:** При ручном клике пользователя вырезать область 100x100, получить текстовую семантику от Qwen-VL, прогнать через Embedding-модель (например, `nomic-embed-vision`) и сохранить в `pgvector`.
- **Критерий приемки:** Успешный `INSERT` визуального эмбеддинга в базу с метаданными.

### [TICKET-3.3] Парсер чертежей (Blueprint Engine)
- **Агент:** 🤖 Automation & 🌐 Fullstack
- **Тип:** Parallel
- **Задача:** Написать State Machine (Конечный автомат), который берет JSON-граф (Blueprint) и последовательно дергает API Песочницы (Найди якорь -> Перемести мышь -> Нажми -> Подожди).

---

## 🚦 Эпик 4: Платформа управления и HITL (The Dashboard)
*Можно делать параллельно остальным задачам.*

### [TICKET-4.1] Разработка Next.js интерфейса
- **Агент:** 🌐 Fullstack
- **Тип:** Parallel
- **Задача:** Создать дашборд (React, Tailwind), виды профилей агентов и iframe для отображения потока WebRTC/VNC.

### [TICKET-4.2] Интеграция биллинга и Vast.ai API
- **Агент:** 🌐 Fullstack
- **Тип:** Parallel
- **Задача:** Логика автоматической аренды GPU на Vast.ai при старте агента и уничтожения при балансе <= 0.

### [TICKET-4.3] Механика Human-in-the-Loop (HITL) и Telegram Бот
- **Агент:** 🌐 Fullstack
- **Тип:** Parallel
- **Задача:** Написать Telegram-бота или Webhook-систему, которая отправляет уведомление пользователю с картинкой ("Агент хочет нажать Опубликовать. Разрешить? [Да/Нет]"). Агент ставится на паузу до получения ответа.

---

## 🚦 Эпик 5: Сборка Цикла Автономии (The Autonomous Loop)
*Финальный этап сборки. Требует готовности Эпиков 1, 2 и 3.*

### [TICKET-5.1] Имплементация Главного Цикла Агента (Orchestrator Loop)
- **Агент:** 🤖 Automation & 👁️ VisionQA
- **Тип:** Sequential
- **Задача:** Связать Восприятие (2.3), RAG (3.2) и Действия (2.1, 2.2). Написать скрипт непрерывного выполнения: `Захват Экрана -> Поиск Якоря в БД -> Обнаружение Координат на Экране через Vision -> Движение мыши -> Клик -> Повтор`.
- **Критерий приемки:** Агент может автономно пройти базовый Blueprint (например, зайти на google.com и вбить слово в поиск) без сбоев.

### [TICKET-5.2] Система Guardrails (Супервизор)
- **Агент:** 👁️ VisionQA
- **Тип:** Sequential
- **Задача:** Внедрить легковесную LLM-проверку намерений перед каждым критическим действием (например, перед вставкой пароля или нажатием Submit).
- **Критерий приемки:** При попытке агента сделать деструктивное действие вне Blueprint'а, скрипт обрывает выполнение и кидает алерт.
